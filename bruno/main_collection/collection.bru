auth {
  mode: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Welcome to the Lernello API!

  ## Environments

  Choose the `Development` environment to get started with the API locally.
  
  ## Authenticated requests
  
  The collection uses the `authToken` variable to send authenticated requests.
  
  The `Login` request is used to obtain the token. If your run it, the Post-Script will automatically set the `authToken` variable for you.
}
